server:
  port: 8090

spring:
  application:
    name: auth
  cloud:
    config:
      uri: ${CONFIG_SERVER_URI}
      username: ${CONFIGURATION_SERVER_USERNAME}
      password: ${CONFIGURATION_SERVER_PASSWORD}
  main:
    allow-circular-references: false




